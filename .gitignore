node_modules/
dist/
coverage/
playwright-report/
test-results/
blob-report/
*.log
.DS_Store
.env
.env.local

# Impeccable local artifacts: critique snapshots, detector cache, live-mode state.
# The design record (DESIGN.md, .impeccable/design.json) is tracked; these are not.
.impeccable/critique/
.impeccable/hook.cache.json
.impeccable/live/
